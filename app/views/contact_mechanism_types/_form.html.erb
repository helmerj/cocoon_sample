<%= form_for(@contact_mechanism_type) do |f| %>
  <% if @contact_mechanism_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_mechanism_type.errors.count, "error") %> prohibited this contact_mechanism_type from being saved:</h2>

      <ul>
      <% @contact_mechanism_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
